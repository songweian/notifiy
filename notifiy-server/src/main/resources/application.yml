# h2 db config
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/notify?useSSL=false&serverTimezone=UTC
    username: root
    password: verysecret
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 127.0.0.1
#  jpa:
#    database-platform: org.hibernate.dialect.H2Dialect
#    hibernate:
#      ddl-auto: create-drop
# h2 init V1_0__common.sql
#spring.datasource.schema: classpath:db-schema/V1_0__common.sql